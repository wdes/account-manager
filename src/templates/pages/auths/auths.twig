{% extends 'base+login.twig' %}

{% block title %}{% trans "Auths" %}{% endblock %}
{% block content %}
    <section class="content-header">
        <h1>
            {% trans "Auths" %}
            <small>{% trans "auths" %}</small>
        </h1>
    </section>

    <section class="content">
        <div class="box">
            <div class="box-header with-border">
              <h3 class="box-title">{% trans "Auths" %}</h3>
              <div class="pull-right col-md-2">
                <a class="btn btn-block btn-primary btn-flat" href="?action=add">{% trans "Add" %}</a>
              </div>
            </div>
            <div class="box-body">
              <table class="table table-bordered">
                <thead>
                    <tr>
                        <th style="width: 10px">#</th>
                        <th>Label</th>
                    </tr>
                </thead>
                <tbody>
                {% for identity in auths.auths() %}
                    <tr>
                        <td>{{ identity.idAuth }}</td>
                        <td>{{ identity.labelAuth }}</td>
                    </tr>
                {% endfor %}
              </table>
            </div>
          </div>
    </section>
{% endblock %}